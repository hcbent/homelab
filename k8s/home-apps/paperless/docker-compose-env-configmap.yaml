apiVersion: v1
data:
  PAPERLESS_OCR_LANGUAGE: eng
  # Retrieve secret key from Vault
  # Vault path: secret/homelab/databases/paperless:secret_key
  # Command: vault kv get -field=secret_key secret/homelab/databases/paperless
  PAPERLESS_SECRET_KEY: VAULT_SECRET_REFERENCE
  PAPERLESS_TIME_ZONE: America/New_York
  PAPERLESS_URL: https://paperless.lab.thewortmans.org
  USERMAP_GID: "1000"
  USERMAP_UID: "1000"
kind: ConfigMap
metadata:
  labels:
    io.kompose.service: webserver-docker-compose-env
  name: docker-compose-env
