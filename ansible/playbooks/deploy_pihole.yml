---
- name: Deploy Pi-hole with Docker
  hosts: pihole
  become: true
  gather_facts: true
  vars:
    pihole_webpassword: "admin123"
    pihole_dns1: "1.1.1.1"
    pihole_dns2: "8.8.8.8"
    pihole_timezone: "America/New_York"

  roles:
    - pihole