# Local .terraform directories
**/.terraform/*

# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files, which are likely to contain sensitive data, such as
# password, private keys, and other secrets. These should not be part of version
# control as they are data points which are potentially sensitive and subject
# to change depending on the environment.
*.tfvars
*.tfvars.json

# Ignore override files as they are usually used to override resources locally and so
# are not checked in
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore transient lock info files created by terraform apply
.terraform.tfstate.lock.info

# Include override files you do wish to add to version control using negated pattern
# !example_override.tf

# Include tfplan files to ignore the plan output of command: terraform plan -out=tfplan
# example: *tfplan*

# Ignore CLI configuration files
.terraformrc
terraform.rc
tf/.terraform.lock.hcl
*.swp

f.f
g.g
k8s/lab-cluster/es/values-default.yaml
k8s/.llm-app.yaml.swp
.ansible/.lock
ELASTIC_PASSWORD
MONITORING_PASSWORD
cchs:ELASTIC_PASSWORD
docker/jackett/config/Jackett
docker/lidarr/config
docker/qbittorrent/config
docker/radarr/config
docker/sonarr/config
docker/unpackerr/config

# Docker Compose files (may contain secrets)
docker-compose*.yml
docker-compose*.yaml
!docker-compose*.yml.example
!docker-compose*.yaml.example

# HashiCorp Vault secrets (CRITICAL - DO NOT COMMIT)
vault/README.md
vault/.vault-token
.vault-token
.vault-secrets/
*vault-init*.txt
*vault-init*.json
vault/scripts/*.backup

# Kubernetes secrets (DO NOT COMMIT)
k8s/helm/values/freenas-nfs.yaml
k8s/helm/values/freenas-iscsi.yaml
k8s/**/secrets.yaml
k8s/**/*-secret.yaml

# Ansible Vault (only commit if encrypted)
.vault_pass
ansible/**/*.backup

# Environment files with secrets
.env
.env.*
*.env
!*.env.example

# Password and key files
*_PASSWORD
*_TOKEN
*_API_KEY
*_api_key
*_SECRET
*.pem
*.key
!*.pub

# Specific secret files (explicit blocks)
CCHS_PASSWORD
makerspace_es_api_key

# Backup files that may contain secrets
*.backup
*.bak
*.old
